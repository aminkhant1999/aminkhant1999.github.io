# 🧠 Mini Memory Game (HTML, CSS & JavaScript)

A clean and modern **memory-matching game** built with **HTML**, **CSS**, and **vanilla JavaScript**.  
The game features animated card flipping, move tracking, match counting, and a polished UI inspired by modern design.

---

## 🚀 Project Overview

This browser-based game works as follows:

1. A 4×4 grid of hidden cards is displayed  
2. Click on two cards to reveal emojis  
3. If they match → They stay flipped  
4. If not → They flip back  
5. The game tracks your **moves** and **matches**  
6. When all 8 pairs are matched → You win!  

Includes a **New Game** button and a **Back to Home** button.

---

## 🧩 Key Concepts Practised

### 🟦 JavaScript (Game Logic)
- Event handling (`click`, `keydown`)
- DOM manipulation
- Card flipping animations
- Move & match counters
- Random shuffling (Fisher–Yates)
- Locking the board during animations

### 🟩 CSS (Modern UI)
- Responsive grid layout  
- Light animations using `transform`  
- Custom color palette using CSS variables  
- Styled buttons and HUD layout  
- Accessible ARIA attributes

### 🟥 HTML (Structure)
- Accessible roles (`application`, `grid`, `gridcell`)
- Semantic header, buttons, and layout wrappers

---

## 🛠️ Technologies Used

- **HTML5**
- **CSS3**
- **JavaScript (ES6)**

---

